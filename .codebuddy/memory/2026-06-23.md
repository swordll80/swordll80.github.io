# 2026-06-23

## 全站页面风格统一重构
- 将所有子页面统一为 **左侧两级树导航 + 右侧内容区（iframe嵌入）** 的布局
- 风格与主页 `index.html` 和 `tools/index.html` 保持一致：毛玻璃 header、径向渐变背景、圆角卡片布局
- 每个页面 header 左侧有 `← 返回主页` 链接（使用 `class="back"` 样式），右侧为页面标题

### 已重构的页面
1. **programming/index.html** — 左侧树：项目/笔记/代码片段（空分类），右侧 iframe 嵌入
2. **education/index.html** — 左侧树：读书笔记/学习心得/教学思考（空分类），右侧 iframe 嵌入
3. **outdoors/index.html** — 左侧树：徒步/旅行/路线（空分类），右侧 iframe 嵌入
4. **classics/index.html** — 左侧树：《聊斋志异》（403篇），点击后在右侧 iframe 中打开 lzzy/index.html
5. **classics/lzzy/index.html** — 左侧树：16卷可展开目录（共403篇古文），点击篇目后在右侧 iframe 嵌入对应古文 HTML
6. **tools/index.html** — header 微调，使用统一的 `class="back"` 样式

### 设计规范
- CSS 变量统一使用 `--bg`, `--panel`, `--text`, `--muted`, `--line`, `--accent`, `--accent-soft`
- 所有子页面引用 `../styles.css`（或 `../../styles.css`）作为共享基础样式
- `styles.css` 精简为基础变量、链接、`.back`、标题等公用样式
- 页面特有的布局 CSS（header/layout/sidebar/tree/iframe）内嵌在各页面 `<style>` 中
- 树导航使用 `data-toggle` 展开/折叠分类，`data-tool` 触发右侧内容加载

## C语言知识表
- 在 `programming/` 下创建了 `c.html`，一份完整的 C 语言知识速查表
- 表格包含4列：序号、知识点、代码示例、版本
- 分为15个章节，共155个条目：
  一、基础语法 (18条)：Hello World、数据类型、运算符、优先级等
  二、控制流 (7条)：if/switch/for/while/goto
  三、数组与字符串 (7条)：一维/多维数组、VLA、字符串函数、转换
  四、指针 (10条)：基本概念、void指针、指针运算、函数指针、const/restrict
  五、函数 (10条)：定义/声明、传值传址、可变参数、内联、递归
  六、动态内存管理 (6条)：malloc/calloc/realloc/free、常见内存错误
  七、结构体/联合体/枚举 (11条)：typedef、对齐、位域、柔性数组、复合字面量
  八、预处理器 (8条)：#include/#define/条件编译/头文件保护/预定义宏
  九、文件I/O (10条)：打开模式、字符/行/格式化/二进制读写、文件定位、错误处理
  十、标准库 (8条)：stdlib/math/time/assert/setjmp/errno/signal
  十一、多文件编程与链接 (4条)：extern/static/编译链接流程
  十二、高级特性 (7条)：_Bool/_Complex/_Atomic/_Generic/_Alignas/thread_local/_Noreturn
  十三、常见Bug与陷阱 (21条)：=vs==/越界/溢出/悬垂指针/格式不匹配等
  十四、编程建议与最佳实践 (20条)：编译警告/const/防御性编程/命名/测试/UB等
  十五、C标准版本差异 (5条)：C89→C99→C11→C17→C23 关键变化
- 在 programming/index.html 的"笔记"分类下添加了 C语言 树节点（count 更新为1）
